// src/pages/Home.tsx
import React from 'react';
import { Typography } from '@material-ui/core';

const Home: React.FC = () => (
    <Typography variant="h4">Welcome to the Digital Profile</Typography>
);

export default Home;
